<div class="max-w-[1140px] mx-auto px-4 ">
  <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    <% books.forEach(function(book) { %>
      <%- include('./listitem', { book }) %>
    <% }) %>
  </ul>
</div>